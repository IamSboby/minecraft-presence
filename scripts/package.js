import fs from 'node:fs/promises';import path from 'node:path';import {execFileSync} from 'node:child_process';import {fileURLToPath} from 'node:url';import crypto from 'node:crypto';
const root=path.dirname(path.dirname(fileURLToPath(import.meta.url)));const dist=path.join(root,'dist');await fs.mkdir(dist,{recursive:true});const stagingRoot=await fs.mkdtemp(path.join(dist,'stage-'));const stage=path.join(stagingRoot,'minecraft-private-presence');await fs.mkdir(stage);
const files=['package.json','package-lock.json','electron-builder.yml','README.md','LICENSE','VALIDATION.md','RUNTIME-SOURCE.md','FILES.md','Start.cmd','.gitignore','.gitattributes','desktop','src','ui','scripts','test','native/ProcessScanner.cs','native/dist/ProcessScanner.exe','sensor/src','sensor/test','sensor/MANIFEST.MF','sensor/dist/presence-sensor.jar','.github'];
for(const file of files)await fs.cp(path.join(root,file),path.join(stage,file),{recursive:true});
const archive=path.join(dist,'minecraft-private-presence-alpha.tar.gz');execFileSync('tar',['-czf',archive,'-C',stagingRoot,'minecraft-private-presence'],{windowsHide:true});
const hash=crypto.createHash('sha256').update(await fs.readFile(archive)).digest('hex');await fs.writeFile(archive+'.sha256',hash+'  '+path.basename(archive)+'\n');console.log(path.basename(archive));
